# Base URL of the existing Ripehouse backend, without a trailing slash.
RHA_BACKEND_URL=https://api.example.com

# Existing backend endpoint for public lead submissions.
RHA_PUBLIC_LEAD_PATH=/api/public/leads
